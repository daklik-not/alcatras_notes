Здесь особо не будет про сами процессы, потому что это надо читать таннебаума, здесь больше паттерн того как это должно выглядеть в нормальной системе.

**Session 0 (сессия 0)** — это изолированная системная среда для запуска фоновых служб и драйверов, а **Session 1 (сессия 1)** — это первая интерактивная сессия, создаваемая для пользователя, который вошел в систему

**Services**  - службы. Они работает в фоновом режиме, чаще всего под SYSTEM правами. Запускаются до того, как пользователь авторизуется.  Например у меня сейчас AmneziaVPN служба запущена. 

***Task Manager - диспетчер задач***
Ну тут проста куча полезной информации, которой все еще не хватает для нормальной работы. 
Лучше пользоваться **Process Hacker**. 
#### Ключевые процессы

##### ***System - PID 4***
Короче говоря, драйвера через процесс system выполняют фоновую работу, например проверка tty на наличие чего то.
![[Cache/Pasted image 20260811170005.png]]
Если как на картинке то все ок.
*Что может быть не так* - другой процесс-родитель, много таких процессов, другой PID.

#### ***smss.exe - Session Manager Subsystem***
#smss 
Первый пользовательский процесс. Запускает процесс Win32 подсистемы(crss.exe) и wininit.exe(запускает некоторые системные службы) в Session 0. В session 1 запускает csrss.exe и winlogon.exe(мененджер входа в систему).
Должно выглядить примерно как на картинке. 
![[Cache/Pasted image 20260811172313.png]]


##### ***csrss.exe - client server runtime process***. 
#crss
пользовательская сторона Win32 подсистемы. Короче windows программы пишутся через Win32 и все так или иначе попадает в csrss.exe. 
![[Cache/Pasted image 20260811175031.png]]
Видно что они есть для разных сеансов.
Для Session 0
![[Cache/Pasted image 20260811175202.png]]
Для Session 1
![[Cache/Pasted image 20260811175224.png]]

#### ***wininit.exe -  Windows Initialization Process***
Запускает services.exe(запускает все службы, которые в свою очередь запускаются в контейнерах svchost.exe), lsass.exe(проверяет мой пароль, получая его от winlogon, выдает токены доступа, хранит учетные данные), lsaiso.exe(в нем хранятся критические компоненты вроде ключей шифрования, паролей, даже при доступе к ядру не получится его прочитать просто так) внутри s0(все в s0). Он запускается один. 

![[Cache/Pasted image 20260811180509.png]]

#### ***services.exe  - Service Control Manager***
Он запускает сервисы, взаимодействует с сервисами, завершает сервисы. Реализует базу данных - sc.exe(получить доступ). `HKLM\System\CurrentControlSet\Services`. - здесь можно посмотреть  информацию о сервисах . Также при авторизации пользователя он меняет  значение HKLM\System\Select\LastKnownGood. Это последняя успешная конфигурация системы. После новой успешной авторизации они заменяются. 
Так должно быть
![[Cache/Pasted image 20260811182328.png]]

#### ***svchost.exe - Service Host***
Этих процессов может быть много.  Каждый такой процесс  обслуживает логическую группу сервисов по назначению.  Он импортирует в свое АП dll файлы всех служб, которые он хочет обслуживать. Dll файл это просто бинарный код, который нужно запустить в каком-то контексте. Какие именно dll находится вот здесь 
```
HKLM\SYSTEM\CurrentControlSet\Services\SERVICE NAME\Parameters\ServiceDLL
```
Или можно посмотреть в *Process Hacker*. 
Сервис должен выглядеть примерно вот так. 

![[Cache/Pasted image 20260813175144.png]]
То что идет после -k в бинарном пути показывает то, какую группу процессов надо будет запустить. 
Ниже то, как должен выглядить легитимный  процесс svchost.exe.
![[Cache/Pasted image 20260813175949.png]]


#### ***lsass.exe - Local Security Authority Subsystem Service***
Это windows процесс, который отвечает за реализацию политики безопасности.  Он подтверждает авторизацию пользователя в систему, создает токены доступа и пишет в Windows Security log. Всегда работает в s0. 
Здесь лежат dll, которые проверяют конкретные протоколы проверки паролей. 
```
HKLM\System\CurrentControlSet\Control\Lsa
```
На картинке, как нормально он выглядит
![[Cache/Pasted image 20260813181131.png]]
 Часто при атаке на lsass использую *mimikatz*, она извлекает из оперативной памяти пароли в в открытом виде. 

#### ***winlogon.exe - Windows Logon***
Отвечает непосредственно за обработку **Secure Attention Sequence - SAS**. Это как я понимаю просто ctrl+alt+del -> вызов защищенного окна ввода пароля. Фактически дает защищенное окошко для ввода пароля, и при правильном вводе загружает в реестр  пользователя, запускает шелл, настраивает переменные окружения, запускает userinit.exe. 
Должно выглядеть примерно вот так
![[Cache/Pasted image 20260813182926.png]]


#### ***explorer.exe***
Дает пользователю доступ к папкам и файлам.  userinit.exe, который запускается от winlogon запускает то что лежит вот здесь.
```
HKLM\Software\Microsoft\Windows NT\CurrentVersion\Winlogon\Shell
```
В нормальном положении это должно быть
![[Cache/Pasted image 20260813183458.png]]
Должно быть как на картинке + не должно быть никаких TCP/IP входящих соединений
![[Cache/Pasted image 20260813183604.png]]
